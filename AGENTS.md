<!-- bmad:context -->
<!-- Verified 2026-09-11 against 162f736. Managed by bmad-project-context; edits inside this block are replaced on refresh. Keep anything you want preserved outside the markers. -->

## Bazaar

Laravel package (`tigaphonic/bazaar`) providing a headless Filament admin backend for an eCommerce Brand Store — catalog/order/finance/etc., new + second-hand/serialized inventory — consumed by a separate customer-facing portal via a Service/API layer the package exposes. Still the unmodified `spatie/package-skeleton-laravel` scaffold; no domain code exists yet. Planning lives in `_artifacts/planning-artifacts/prds/prd-Tigaphonic/bazaar-2026-09-11/`.

## Where things are

- Product requirements: `prd.md` in the planning folder above (9 domains, FR-1–FR-40, status final).
- Technical addendum: `addendum.md` in the same folder — architecture principles (P1–P10), schema detail, gateway choices (RajaOngkir shipping, Midtrans Core API payment). **Belum mengikat**: eksplisit berstatus input ke `bmad-architecture`, yang belum dijalankan. Jalankan ulang skill ini setelah dokumen arsitektur ada, untuk mempromosikan keputusan yang sudah settle ke sini.
- Original product brief: `docs/draft/brandstore-package-brief.md`.

<!-- /bmad:context -->
